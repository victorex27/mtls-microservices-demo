aws_region       = "us-east-1"
project_name     = "mtls-demo"
instance_type    = "t3.medium"

# REQUIRED, no default on purpose - see variables.tf. Find your IP with:
#   curl -s https://checkip.amazonaws.com
ssh_allowed_cidr = "203.0.113.4/32"
